func sumList(lst){
    var length = len(lst)
    var sum = 0
    for i = 0 to length:
        var aux = get(lst, i)
        var sum = sum + aux
    end
    return sum
}

var lst = [1, 2, 3, 4, 5]
print(sumList(lst))
